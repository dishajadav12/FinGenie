{
  "name": "fin-genie",
  "version": "1.0.0",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "NODE_ENV=development node server.js",
    "reset": "node -e \"require('fs').existsSync('fin_genie.db') && require('fs').unlinkSync('fin_genie.db')\" && node server.js"
  },
  "dependencies": {
    "better-sqlite3": "^9.0.0",
    "dotenv": "^16.3.1",
    "express": "^4.19.0",
    "node-fetch": "^3.3.2",
    "ws": "^8.14.2"
  }
}
