# FastAPI
APP_ENV=dev
PORT=8000

# CORS: your frontend origin(s), comma-separated (no trailing slashes)
CORS_ORIGINS=http://localhost:5173,https://your-vercel-app.vercel.app

# DB (SQLite file for hackathon)
DATABASE_URL=sqlite:///./billsense.db
